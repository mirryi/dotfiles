globals = {
    pkg = {
        name,
        dependencies,
        files = {
            trees, extra, templates, link_type, replace_files, replace_dirs
        },
        hooks = {pre, install, post},
        variables
    }
}
