#!/usr/bin/env bash
# Mullvad connection state: connecting | connected | disconnected. The yuck
# layer maps state -> glyph + colour class.
case "$(mullvad status 2>/dev/null)" in
*Connecting*) echo connecting ;;
*Connected*)  echo connected ;;
*)            echo disconnected ;;
esac
