#!/usr/bin/env bash
# Mullvad state as JSON: { icon, cls }. cls drives the colour (on/mid/off).
case "$(mullvad status 2>/dev/null)" in
*Connecting*)   printf '{"icon":"󰔟","cls":"mid"}' ;;
*Connected*)    printf '{"icon":"󰦝","cls":"on"}' ;;
*)              printf '{"icon":"󰦞","cls":"off"}' ;;
esac
