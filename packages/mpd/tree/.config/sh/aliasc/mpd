#!/bin/sh
# update mpd database
alias mpcup="mpc update"

# mpc shortcuts
alias mpcp="mpc toggle"
alias mpcl="mpc load"
alias mpccl="mpc clear"
alias mpcr="mpc repeat"
alias mpcz="mpc random"
alias mpcc="mpc consume"
