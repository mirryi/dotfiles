# X11 portal policy: route portal interfaces to X11-capable backends.
# Screenshot has no X11 backend by default (gtk doesn't implement it),
# so point it at lxqt, which grabs via Xlib. Requires xdg-desktop-portal-lxqt.
[preferred]
default=gtk
org.freedesktop.portal.Screenshot=lxqt
