# WARNING: Do not edit this file.
# It was generated by processing a template.

[mgr]
cwd = { fg = "{{ colors.normal.cyan }}" }

hovered         = { fg = "{{ colors.primary.background }}", bg = "{{ colors.normal.blue }}" }
preview_hovered = { underline = true }

find_keyword  = { fg = "{{ colors.bright.yellow }}", italic = true }
find_position = { fg = "{{ colors.bright.magenta }}", bg = "reset", italic = true }

marker_copied   = { fg = "{{ colors.normal.green }}", bg = "{{ colors.normal.green }}" }
marker_cut      = { fg = "{{ colors.normal.red }}", bg = "{{ colors.normal.red }}" }
marker_marked   = { fg = "{{ colors.normal.blue }}", bg = "{{ colors.normal.blue }}" }
marker_selected = { fg = "{{ colors.bright.yellow }}", bg = "{{ colors.bright.yellow }}" }

count_copied   = { fg = "{{ colors.primary.background }}", bg = "{{ colors.normal.green }}" }
count_cut      = { fg = "{{ colors.primary.background }}", bg = "{{ colors.normal.red }}" }
count_selected = { fg = "{{ colors.primary.background }}", bg = "{{ colors.bright.yellow }}" }

border_symbol = "│"
border_style  = { fg = "{{ colors.normal.black }}" }

[mode]
normal_main = { fg = "{{ colors.primary.background }}", bg = "{{ colors.normal.blue }}", bold = true }
normal_alt  = { fg = "{{ colors.normal.blue }}", bg = "{{ colors.primary.background }}" }
select_main = { fg = "{{ colors.primary.background }}", bg = "{{ colors.normal.green }}", bold = true }
select_alt  = { fg = "{{ colors.normal.green }}", bg = "{{ colors.primary.background }}" }
unset_main  = { fg = "{{ colors.primary.background }}", bg = "{{ colors.normal.magenta }}", bold = true }
unset_alt   = { fg = "{{ colors.normal.magenta }}", bg = "{{ colors.primary.background }}" }

[status]
overall  = { fg = "{{ colors.primary.foreground }}" }
progress_label  = { fg = "{{ colors.primary.foreground }}", bold = true }
progress_normal = { fg = "{{ colors.normal.blue }}", bg = "{{ colors.primary.background }}" }
progress_error  = { fg = "{{ colors.primary.alarm }}", bg = "{{ colors.primary.background }}" }

[input]
border   = { fg = "{{ colors.normal.blue }}" }
selected = { reversed = true }

[pick]
border   = { fg = "{{ colors.normal.blue }}" }
active   = { fg = "{{ colors.bright.magenta }}", bold = true }

[confirm]
border  = { fg = "{{ colors.normal.blue }}" }
title   = { fg = "{{ colors.normal.blue }}" }

[cmp]
border = { fg = "{{ colors.normal.blue }}" }

[tasks]
border  = { fg = "{{ colors.normal.blue }}" }
title   = { fg = "{{ colors.normal.blue }}" }
hovered = { fg = "{{ colors.bright.magenta }}", underline = true }

[which]
mask            = { bg = "{{ colors.primary.background }}" }
cand            = { fg = "{{ colors.normal.cyan }}" }
rest            = { fg = "{{ colors.bright.black }}" }
desc            = { fg = "{{ colors.bright.magenta }}" }
separator_style = { fg = "{{ colors.normal.black }}" }

[help]
on      = { fg = "{{ colors.bright.magenta }}" }
run     = { fg = "{{ colors.normal.cyan }}" }
hovered = { reversed = true, bold = true }
footer  = { fg = "{{ colors.primary.foreground }}", bg = "{{ colors.primary.background }}" }

[git]
modified  = { fg = "{{ colors.normal.yellow }}" }
added     = { fg = "{{ colors.normal.green }}" }
untracked = { fg = "{{ colors.bright.black }}" }
ignored   = { fg = "{{ colors.bright.black }}" }
deleted   = { fg = "{{ colors.normal.red }}" }
updated   = { fg = "{{ colors.normal.yellow }}" }
